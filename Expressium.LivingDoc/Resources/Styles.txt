<style>
    :root {
        
        /* Bootstrap Grayscale */
        --bs-gray-075: #fdfdfe;
        --bs-gray-075: #fefefe;
        --bs-gray-090: #f9fafb;
        --bs-gray-100: #f8f9fa;
        --bs-gray-125: #f6f7f8;
        --bs-gray-150: #f1f3f5;
        --bs-gray-200: #e9ecef;
        --bs-gray-300: #dee2e6;
        --bs-gray-400: #ced4da;
        --bs-gray-450: #bec5cc;
        --bs-gray-500: #adb5bd;
        --bs-gray-600: #6c757d;
        --bs-gray-650: #565d63; /* 5b636a */;
        --bs-gray-700: #495057;
        --bs-gray-800: #343a40;
        --bs-gray-900: #212529;

        /* Header */
        --color-text-header: var(--bs-gray-900);
        --color-bg-header: #dfe7f2; /* #d4dfec */

        /* Footer */
        --color-text-footer: var(--bs-gray-600);
        --color-bg-footer: var(--bs-gray-100);

        /* Navigation */
        --color-text-navigation: var(--bs-gray-900);
        --color-bg-navigation: var(--bs-gray-100);
        --color-bg-navigation-indicator: var(--bs-gray-500);

        /* Tables */
        --color-bg-table-header: var(--bs-gray-100);
        --color-border-table-header: var(--bs-white);

        /* Groups */
        --color-bg-group: var(--bs-gray-075);
        --color-border-group: #ECECEC;

        /* Outlines */
        --color-border-grid: #ECECEC;
        --color-border-splitter: #ECECEC;

        /* Surfaces */
        --color-bg-hover: #F5F8FC;
        --color-bg-selected: #EEF4FA;

        /* Scrollbars */
        --color-text-scrollbar: #969696;
        --color-bg-scrollbar: var(--bs-white);

        /* Texts */
        --color-text: var(--bs-gray-900);
        --color-text-darkgray: var(--bs-gray-700);
        --color-text-gray: var(--bs-gray-600);

        /* Special Texts */
        --color-text-keyword: #25576b;
        --color-text-link: #295f75;

        /* Tags */
        --color-text-tag: #338689;
        --color-bg-tag: #eef4f8;

        /* Badges */
        --color-text-badge: var(--bs-white);
        --color-bg-badge: #969696;

        /* Buttons */
        --color-text-button: var(--bs-gray-900);
        --color-bg-button: var(--bs-gray-090);
        --color-border-button: var(--bs-gray-400);
        --color-text-button-inactive: var(--bs-gray-600);
        --color-bg-button-inactive: var(--bs-gray-090);
        --color-border-button-inactive: var(--bs-gray-300);
        --color-text-button-hover: var(--bs-gray-900);
        --color-bg-button-hover: var(--bs-gray-100);
        --color-border-button-hover: var(--bs-gray-500);
        --color-bg-button-selected: var(--color-bg-selected);

        /* Status colors */
        --color-passed: #47A64E;
        --color-incomplete: #F7C21A;
        --color-failed: #E84330;
        --color-skipped: #9E9E9E;

        --color-passed: #45A14B;
        --color-incomplete: #F5C01A;
        --color-failed: #E6422F;
        --color-skipped: #9C9C9C;

        --color-total: var(--bs-gray-900);

        /* Status backgrounds */
        --color-bg-passed: #EAF6EB;
        --color-bg-incomplete: #FFF6D8;
        --color-bg-failed: #FAE8E5;
        --color-bg-skipped: #F0F2F4;

        /* Status texts */
        --color-text-passed: #2C5F30;
        --color-text-incomplete: #8A6500;
        --color-text-failed: #9E2B21;
        --color-text-skipped: var(--bs-gray-700);
    }
   
    body, html {
        height: 100%;
        margin: 0;
        overflow: hidden;
        font-size: 16px;
        color: var(--color-text);
        /* font-family: system-ui, "Segoe UI Variable Small", "Segoe UI", Roboto, "Helvetica Neue", "Noto Sans", "Liberation Sans", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"; */
    }
   
    header {
        padding: 0.50em 1.00em;
        color: var(--color-text-header);
        background-color: var(--color-bg-header);
    }

    .project-name {
        font-size: 1.375em;
        font-weight: bold;
    }

    .project-date {
        color: var(--color-text-darkgray);      
        padding-left: 0.125em;
    }

    .navigation {
        padding: 0.50em 1.00em;
        background-color: var(--color-bg-navigation);
        border-bottom: 1px solid var(--color-border-splitter);
    }

    a.navigation-link {
        color: var(--color-text-navigation);
        position: relative;
        padding-left: 0.0625em;
        padding-right: 0.0625em;
        margin-right: 0.75em;
        font-size: 1.0625em;
        text-decoration: none; /* remove default underline */
    }

    a.navigation-link.active {
        font-weight: 600;
        font-size: 1.125em;
    }

    a.navigation-link.active::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -0.60em; /* adjust spacing under text */
        width: 100%;
        height: 2px;  /* thickness of line */
        background-color: var(--color-bg-navigation-indicator);
    }

    .splitter-wrapper {
        display: flex;
        height: calc(100% - 9.75em); /* Adjust for header and footer height */
        flex-direction: row; /* Change to row for vertical splitter */
        border-bottom: 1px solid var(--color-border-splitter);
    }

    #splitter {
        width: 2px;
        background: var(--color-border-splitter);
        cursor: col-resize;
    }

    #splitter-left {
        flex: 1;
        overflow: hidden;
        scrollbar-color: var(--color-text-scrollbar) var(--color-bg-scrollbar);
    }

    #splitter-right {
        flex: 1;
        overflow: auto;
        scrollbar-color: var(--color-text-scrollbar) var(--color-bg-scrollbar);
    }

    footer {
        background-color: var(--color-bg-footer);
        text-align: center;
        padding: 0.50em;
    }

    footer a {
        color: var(--color-text-footer);
        font-size: 0.875em;
    }

    .section {
        padding-bottom: 0.50em;
    }

    .scenario-section-body {
        padding-top: 0.25em;
    }

    .table-scroll {
        max-width: 100%;
        overflow-x: auto;
        overflow-y: hidden;
    }

    table.scenario-datatable {
        width: max-content;
        min-width: 100%;
    }

    table.scenario-datatable caption {
        text-align: left;
        caption-side: top;
    }

    .feature-comments,
    .background-comments,
    .rule-comments,
    .scenario-comments,
    .examples-comments {
        white-space: pre-wrap;
        list-style: none;
        margin: 0.25em 0;
        padding-left: 0;
        color: var(--color-text-gray);
    }

    .layout-row {
        display: flex;
        width: 100%;
    }

    .layout-column {
        flex: auto;
    }

    .align-left {
        text-align: left;
    }

    .align-center {
        text-align: center;
    }

    .align-right {
        text-align: right;
    }

    .filter-group {
        background-color: var(--color-bg-group);
        padding: 0.5em;
        margin-top: 0.125em;
        margin-bottom: 0.50em;
        align-items: center;
        border-radius: 1.25em;
        border: 1px solid var(--color-border-group);
    }

    .filter-symbol {
        padding-left: 0.25em;
        padding-right: 0.375em;
        color: var(--color-text-gray);
    }

    .filter-keywords {
        width: 100%;
        box-sizing: border-box;
        border-width: 1px;
        outline: none;
        background-color: var(--color-bg-group);
        border: hidden;
    }

    #filter-list {
        flex: 1;
        overflow: auto;
        height: calc(100% - 4.75em); /* Adjust for filter and search height */
        padding-right: 0.375em;
    }

    th, td {
        padding: 4px 4px;
    }

    th {
        background-color: var(--color-bg-table-header);
        border: 1px solid var(--color-border-table-header);
        font-weight: 600;
    }
    
    table.tree-view {
        width: 100%;
    }

    table.list-view {
        width: 100%;
        margin-top: 0.125em;
    }

    table.list-view tr:hover,
    table.tree-view tr:hover td.grid-border {
        background-color: var(--color-bg-hover);
    }

    table.list-view th,
    table.list-view td {
        padding-left: 6px;
        padding-right: 6px;
    }
   
    table.tree-view td {
        padding-left: 2px;
        padding-right: 2px;
    }

    table.tree-view td.grid-border { 
        padding-left: 4px;
        padding-right: 2px;
    }

    table.tree-view tr.active td.grid-border {
        background-color: var(--color-bg-selected);
    }

    table.list-view tr.active {
        background-color: var(--color-bg-selected);
    }

    /* Buttons */
    button {
        color: var(--color-text-button);
        border: 1px solid var(--color-border-button);
        background-color: var(--color-bg-button);
        border-radius: 0.5em;
        padding: 2px 6px;
    }

    button.filter-option {
        display: inline-flex;              /* enables centering */
        align-items: center;               /* vertical centering */
    }

    /* Hover Buttons */
    button:hover {
        color: var(--color-text-button-hover);
        border: 1px solid var(--color-border-button-hover);
        background-color: var(--color-bg-button-hover);
    }

    /* Inactive Option Buttons */
    button.filter-option:not(.active) {
        color: var(--color-text-button-inactive);
        border: 1px solid var(--color-border-button-inactive);
        background-color: var(--color-bg-button-inactive);
    }

    /* Hover Option Buttons */
    button.filter-option:hover {
        color: var(--color-text-button-hover);
        border: 1px solid var(--color-border-button-hover);
        background-color: var(--color-bg-button-selected);
    }

    /* Active Option Buttons */
    button.filter-option.active {
        color: var(--color-text-button);
        border: 1px solid var(--color-border-button);
        background-color: var(--color-bg-button-selected);
    }

    .grid-folder {
        text-align: center;
        width: 1.5em;
    }

    .grid-folder-name {
        color: var(--color-text-gray);
        font-weight: 500;
    }

    .grid-expand,
    .grid-collapse,
    .scenario-stacktraces,
    .scenario-attachments,
    .scenario-history,
    .scenario-backgrounds {
        padding: 1px 4px;
        font-size: 0.9625em;
        color: var(--color-text-button-inactive);
    }

    .grid-border {
        border-bottom: 1px solid var(--color-border-grid);
    }

    .grid-toggle {
        color: var(--color-text-gray);
        cursor: pointer;
        text-align: center;
        width: 1.5em;
    }
    
    .grid-indent {
        width: 1.25em;
    }

    .page-name {
        font-size: 1.375em;
        font-weight: 600;
        line-height: 1;
    }

    .chart-name {
        font-size: 1.25em;
        font-weight: 600;
        line-height: 1.25;
    }

    .chart-outline {
        width: fit-content;
        margin: auto;
        padding: 1.5em 2.0em;
        background-color: var(--color-bg-group);
        border-radius: 1.00em;
        border: 1px solid var(--color-border-group);
    }

    .chart-filter {
        padding: 1.0em 1.0em;
        padding-bottom: 0.5em;
        background-color: var(--color-bg-group);
        border-radius: 1.00em;
        border: 1px solid var(--color-border-group);
        margin-bottom: 0.50em;
    }

    .chart-status {
      width: 160px;
      height: 164px;
    }

    .chart-number {
        font-size: 0.5em;
        text-anchor: middle;
        transform: translateY(-0.25em);
    }

    .chart-label {
        font-size: 0.325em;
        text-anchor: middle;
        transform: translateY(0.7em);
    }

    .chart-text {
        transform: translateY(0.15em);
    }

    .chart-count {
        text-align: center;
    }

    .chart-count-number {
        font-size: 1.25em;
        line-height: 1.0;
    }

    .chart-count-percentage {
        font-size: 1.125em;
        line-height: 1.0;
    }

    .chart-count-status {
        font-size: 1.125em;
        line-height: 1.0;
    }

    .chart-count-bar {
        width: 110px;
        height: 0.475em;
    }


    .chart-health {
        text-align: center;
    }

    .chart-health-number {
        font-size: 1.125em;
    }

    .chart-health-percentage {
        font-size: 1.0625em;
    }

    .chart-health-status {
        font-size: 1.0625em;
    }

    .chart-health-bar {
        width: 96px;
        height: 0.475em;
    }

    a {
        color: var(--color-text-link);
        text-decoration: none;
    }

    .color-passed {
        color: var(--color-passed);
    }

    .bgcolor-passed {
        background: var(--color-passed);
    }

    .color-incomplete {
        color: var(--color-incomplete);
    }

    .bgcolor-incomplete {
        background: var(--color-incomplete);
    }

    .color-failed {
        color: var(--color-failed);
    }

    .bgcolor-failed {
        background: var(--color-failed);
    }

    .color-skipped {
        color: var(--color-skipped);
    }

    .bgcolor-skipped {
        background: var(--color-skipped);
    }

    .color-total {
        color: var(--color-total);
    }

    .bgcolor-total {
        background: var(--color-total);
    }

    .color-health {
        color: var(--color-text-darkgray);
    }

    .bgcolor-health {
        background: #A7A7A7;
    }

    .status-dot {
        height: 10px;
        width: 10px;
        border-radius: 50%;
        display: inline-block;
        margin-left: 2px;
        margin-right: 2px;
    }

    .status-pill {
      display: inline-flex;              /* enables centering */
      align-items: center;               /* vertical centering */
      justify-content: center;           /* horizontal centering */
      color: white;
      font-size: 0.8125em;
      font-weight: 600;
      padding: 0.0625em 0.625em;
      border-radius: 999px;
      /* margin-right: 0.375em; */
    }

    .status-symbol {
        font-size: 1.0em;
        padding-right: 2px;
        padding-left: 2px;
    }

    button.filter-option .status-symbol {
        margin-right: 3px;
    }

    .health-symbol {
        color: var(--color-text-gray);
    }

    .clear-symbol {
        color: var(--color-text-gray);
        font-weight: 600;
    }

    .sort-column {
        float: right;
        font-weight: normal;
    }

    ul {
        list-style-type: none;
        margin-bottom: 0px;
    }

    .donut-segment-passed {
        stroke: var(--color-passed);
        fill: transparent;
        stroke-width: 0.21em;
    }

    .donut-segment-incomplete {
        stroke: var(--color-incomplete);
        fill: transparent;
        stroke-width: 0.21em;
    }

    .donut-segment-failed {
        stroke: var(--color-failed);
        fill: transparent;
        stroke-width: 0.21em;
    }

    .donut-segment-skipped {
        stroke: var(--color-skipped);
        fill: transparent;
        stroke-width: 0.21em;
    }

    hr {
        border: 0 none;
        margin-top: 0.50em;
        margin-bottom: 0.50em;
    }

    .feature-tag,
    .rule-tag,
    .scenario-tag,
    .scenario-tag-health {
        color: var(--color-text-tag);
        background: var(--color-bg-tag);
        border-radius: 0.375em;
        padding-top: 3px;
        padding-bottom: 4px;
        padding-left: 7px;
        padding-right: 8px;
        font-size: 0.9375em;
    }

    .feature-tag-group,
    .rule-tag-group,
    .scenario-tag-group {
        padding-bottom: 0.25em;
    }

    .feature-keyword,
    .background-keyword,
    .rule-keyword,
    .scenario-keyword,
    .step-keyword {
        color: var(--color-text-keyword);
        font-weight: 600;
    }

    .feature-name,
    .rule-name,
    .scenario-name {
        font-weight: 600;
    }

    .feature-description,
    .rule-description,
    .scenario-description {
        font-size: 0.9375em;
        color: var(--color-text-gray);
        font-style: italic;
        padding-top: 2px;
    }

    .feature-duration,
    .scenario-duration {
        color: var(--color-text-gray);
        padding-left: 4px;
        padding-right: 5px;
        font-size: 0.9375em;
    }

    .scenario-badge {
        width: 1.80em;
        height: 1.80em;
        font-weight: 600;
        border-radius: 50%;
        color: var(--color-text-badge);
        background-color: var(--color-bg-badge);
        text-align: center;
        line-height: 1.80em; /* Centers the text vertically */
        font-size: 0.8125em;
        display: inline-block; /* Allows the circle to be displayed inline */
        margin-left: 3px;
    }

    .step-symbol {
        font-weight: bold;
        text-align: center;
    }

    .scenario-steps {
        padding-top: 2px;
        padding-bottom: 2px;
    }

    ul.scenario-steps li {
        padding-top: 2px;
        padding-bottom: 2px;
    }

    .steps-datatable {
        padding-left: 32px;
    }

    .scenario-datatable {
        color: var(--color-text-gray);
    }

    table.scenario-datatable td {
        padding-top: 1px;
        padding-bottom: 1px;
        padding-left: 4px;
        padding-right: 4px;
    }

    .message-box {
        padding-top: 4px;
        padding-bottom: 4px;
        padding-left: 2px;
        padding-right: 2px;
        max-width: inherit;
        word-wrap: break-word;
    }

    .message-header {
        font-weight: bold;
    }

    .message-passed {
        color: var(--color-text-passed);
        background-color: var(--color-bg-passed);
        padding: 8px;
        border-left: 8px solid var(--color-passed);
    }

    .message-failed {
        color: var(--color-text-failed);
        background-color: var(--color-bg-failed);
        padding: 8px;
        border-left: 8px solid var(--color-failed);
    }

    .message-incomplete {
        color: var(--color-text-incomplete);
        background-color: var(--color-bg-incomplete);
        padding: 8px;
        border-left: 8px solid var(--color-incomplete);
    }

    .message-skipped {
        color: var(--color-text-skipped);
        background-color: var(--color-bg-skipped);
        padding: 8px;
        border-left: 8px solid var(--color-skipped);
    }

    .message-stacktrace {
        font-size: 0.875em;
    }

    .examples-keyword {
        font-weight: 600;
        color: var(--color-text-gray);
        padding-left: 16px;
    }

    .examples-name {
        color: var(--color-text-gray);
    }

    .examples-description {
        font-size: 0.9375em;
        color: var(--color-text-gray);
        font-style: italic;
        padding-left: 32px;
    }

    .examples-datatable {
        padding-left: 32px;
    }

    .history,
    .attachments {
        padding-top: 4px;
    }

    .history-keyword,
    .attachments-keyword {
        font-weight: 600;
        color: var(--color-text-gray);
        padding-left: 16px;
    }

    ul.history-files li,
    ul.attachments-files li {
        padding-top: 1px;
        padding-bottom: 1px;
    }

    .history-files li {
        color: var(--color-text-darkgray);
    }

    .attachment-symbol {
        color: var(--color-text-gray);
    }

    .duration-symbol {
        color: var(--color-text-gray);
        font-size: 2.5em;
    }

    .duration-text {
        font-size: 1.25em;
        padding-left: 8px;
        padding-top: 4px;
        color: var(--color-text-gray);
    }

    .duration-keyword {
        color: var(--color-text-gray);
    }

    .history-passed {
        color: var(--color-text-passed);
        background-color: var(--color-bg-passed);
        text-align: center;
        width: 100px;
    }

    .history-failed {
        color: var(--color-text-failed);
        background-color: var(--color-bg-failed);
        text-align: center;
        width: 100px;        
    }

    .history-incomplete {
        color: var(--color-text-incomplete);
        background-color: var(--color-bg-incomplete);
        text-align: center;
        width: 100px;
    }

    .history-skipped {
        color: var(--color-text-skipped);
        background-color: var(--color-bg-skipped);
        text-align: center;
        width: 100px;
    }

    .history-unknown {
        text-align: center;
        width: 100px;
    }

    .analytics-trends,
    .analytics-healths {
        padding-left: 16px;
        padding-right: 16px;
        margin: auto;
    }

    .analytics-list {
        width: 100%;
    }

    table.analytics-list th,
    table.analytics-list td {
        padding-left: 6px;
        padding-right: 6px;
    }

    table.analytics-list td {
        padding-top: 3px;
        padding-bottom: 3px;
    }

    .analytics-list td {
        border: 1px solid var(--color-border-table-header);
    }

    .card {
        width: 100%;
        height: 100%;
        border: none;
        padding: 1.0em;
        padding-bottom: 0.0em;
    }
</style>